{
  "framework": null,
  "buildCommand": "npm run vercel-build",
  "functions": { "api/server.js": { "runtime": "nodejs20.x" } },

  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server.js" }
  ]
}
